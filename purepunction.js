// Impure function: Modifies a global variable and logs to the console
// let total = 0;
// function addToTotal(value) {
//     total += value;
//     console.log(`Total is now ${total}`);
// }
// addToTotal(5)
// addToTotal(5)
// addToTotal(6)


function addToTotal(value) {
 
    console.log(`Total is now ${value}`);
}
addToTotal(5)
addToTotal(5)
addToTotal(6)
let total=0
function addToTotals(value) {
  total+=value
    console.log(`Total is now ${total}`);
}
addToTotals(5)
addToTotals(5)
addToTotals(6)